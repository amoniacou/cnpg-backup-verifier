---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: backupverifiers.cnpg.k8s.amoniac.eu
spec:
  group: cnpg.k8s.amoniac.eu
  names:
    kind: BackupVerifier
    listKind: BackupVerifierList
    plural: backupverifiers
    singular: backupverifier
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BackupVerifier is the Schema for the backupverifiers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupVerifierSpec defines the desired state of BackupVerifier
            properties:
              cluster:
                description: Cluster is a name of CloudNativePG Cluster which backups
                  we need to validate
                properties:
                  name:
                    description: Name of the referent.
                    type: string
                required:
                - name
                type: object
            required:
            - cluster
            type: object
          status:
            description: BackupVerifierStatus defines the observed state of BackupVerifier
            properties:
              state:
                description: |-
                  What is the state of such verifier.
                  Could be a next states:
                  * validated - when we have found cluster and all backups are valid
                  * checking - when we have started to check all backups
                  * failed - when we have one or more backups invalid
                  * stopped - there is no backup section inside cluster so no need to do anything
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
